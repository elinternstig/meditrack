import { Text } from 'react-native';
import { ScrollView } from 'react-native';

export default function HomeScreen() {
  return (
    <ScrollView>
      <Text>Veckodagar med datum</Text>
      <Text>Alla mediciner</Text>
      <Text></Text>
    </ScrollView>
  );  
}
